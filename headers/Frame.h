#include "./definitions.h"

class frame {
    u4 end_retorno;  // Verificar se é realmente endereço de retorno
    // Pilha_operandos *p;
    // Vetor_locais *v;
    u2 vetor_length;
    // Cp_info *cp;
    char *classeCorrente;
};